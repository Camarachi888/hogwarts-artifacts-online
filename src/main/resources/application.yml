spring:
  profiles:
    active: dev #default active profile. profile will be changed to prod in production due to Azure's env variables
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
api:
  endpoint:
    base-url: /api/v1

server:
  port: 80